#include <stdio.h>
main(){
	int N,kt=1;
	do{
		printf ("Nhap so nguyen N (0 < N < 100000):");
   		scanf("%d",&N);
	}
   	while(N<=0||N>=100000);
	printf ("So nguoc cua so vua nhap la:");
	while(N!=0){
		printf("%d",N%10);
		N=N/10;
	}
}
